# Private input area

Raw empirical archives are restored locally with `python -m thesis_repro data restore` and are ignored by Git. The generated `input_receipt.json` records file names, sizes, and SHA-256 hashes.

