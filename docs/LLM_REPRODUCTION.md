# LLM reproduction

The product plan generates 48,300 unique logical request identities from two
24,150 regimes. Provider calls are disabled by default. Strict and Repaired
are downstream policies over one raw response stream. See
`docs/LLM_CONTRACT_GAP.md` for the unrecovered source snapshot boundary.

