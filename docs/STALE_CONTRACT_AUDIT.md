# Stale contract audit

Historical materials contain 24,150-request pre-live designs, 34,500-request development traces, Plan-3 terminology, and older action nomenclature. These are retained only under `frozen/` or the curated compatibility source tree.

Active product contracts use:

- 48,300 raw logical generations;
- 96,600 Stage8 observations from one raw generation stream evaluated under Strict and Repaired policies;
- 9 candidate actions and 8 managerial dimensions;
- fresh C3-E binding per run.

The LLM contract remains `DECLARED_COUNT_SOURCE_SNAPSHOT_GAP` until the exact historical provider request snapshot and raw parent payloads are restored.

