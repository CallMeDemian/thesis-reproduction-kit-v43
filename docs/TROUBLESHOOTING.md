# Troubleshooting

Start with `doctor`, then `data doctor`, then `status --run-id`. A changed scientific contract invalidates downstream stages. Live LLM execution requires the exact environment gate and remains blocked while the source-snapshot gap is open.

