# Result comparison

Comparison reports use `UNAVAILABLE`, `RECOMPUTED`, `FROZEN_ORIGINAL`, and `COMPARISON_ONLY` statuses. Equal numerical values are not required for a successful replication comparison.

