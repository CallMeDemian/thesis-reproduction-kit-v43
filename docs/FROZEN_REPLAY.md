# Frozen replay

`verify` and `frozen-replay` check the distribution ZIP, canonical Stage8 member, and 914-row registry by SHA-256. The frozen lane is evidence replay; it does not write fresh scientific results.

