# Quickstart

Install the package, run `doctor`, verify the frozen distribution, then use a fresh run in `smoke` profile. Full raw-data restoration and provider execution are separate explicit steps.

