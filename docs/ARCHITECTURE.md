# Architecture

The product has frozen, fresh, and comparison authorities. Frozen artifacts are immutable evidence; a fresh run owns every newly computed parent; the comparison layer joins them without overwriting either authority.

